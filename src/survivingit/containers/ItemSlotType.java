package survivingit.containers;

/**
 * Enum class for what type of ItemSlot an itemSlot is.
 * Used to allow the ability to create itemSlots for specific item types, but currently only allows for itemSlots of
 * type ANY.
 */
public enum ItemSlotType {
    /**
     * Item slot type for all items
     */
    ANY
}
